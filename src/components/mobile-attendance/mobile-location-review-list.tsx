import { LoadingButton } from "@/components/ui/async-feedback";

export function MobileLocationReviewList({ items, action }: { items: Array<{ id: string; employee: string; date: string; title: string; description: string }>; action: (formData: FormData) => void | Promise<void> }) {
  if (items.length === 0) return null;
  return <section className="mb-5 rounded-xl border bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold">Pendências de localização</h2><p className="mt-1 text-sm text-[var(--muted-foreground)]">O horário foi preservado. Decida somente sobre a revisão da localização.</p><ul className="mt-4 divide-y">{items.map((item) => <li className="py-4" key={item.id}><p className="font-semibold">{item.title} · {item.employee}</p><p className="mt-1 text-sm text-[var(--muted-foreground)]">{item.description} · {item.date}</p><form action={action} className="mt-3 grid gap-2 lg:grid-cols-[1fr_auto_auto_auto]"><input name="inconsistencyId" type="hidden" value={item.id} /><input className="input" name="reason" minLength={3} placeholder="Justificativa para revisar ou registrar decisão" /><LoadingButton className="border bg-white text-slate-800 hover:bg-slate-50" loadingLabel="Aprovando…" name="action" value="APPROVE">Aprovar</LoadingButton><button className="rounded-md border px-3 py-2 text-sm font-semibold" name="action" type="submit" value="JUSTIFY">Justificar</button><button className="rounded-md border px-3 py-2 text-sm font-semibold" name="action" type="submit" value="KEEP_IN_REVIEW">Manter em revisão</button></form></li>)}</ul></section>;
}
