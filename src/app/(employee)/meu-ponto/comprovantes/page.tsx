import { formatInTimeZone } from "date-fns-tz";
import { getEmployeeMobileRecords } from "@/modules/mobile-attendance/application/mobile-attendance-service";

export default async function EmployeeReceiptsPage() {
  const { access, punches } = await getEmployeeMobileRecords();
  return <div><p className="eyebrow text-[var(--primary)]">COMPROVANTES</p><h1 className="font-display mt-2 text-5xl font-semibold leading-none text-[var(--foreground)]">Meus comprovantes</h1><p className="mt-3 max-w-xl text-sm leading-6 text-[var(--muted-foreground)]">Registros internos disponíveis para consulta.</p><div className="mt-7 space-y-3">{punches.length === 0 ? <p className="surface rounded-[1.5rem] p-5 text-sm text-[var(--muted-foreground)]">Nenhum comprovante disponível.</p> : punches.map((punch) => <article className="surface rounded-[1.5rem] p-5 sm:p-6" key={punch.id}><div className="flex items-start justify-between gap-4"><div><p className="eyebrow text-[var(--muted-foreground)]">REGISTRO</p><p className="font-display numeric mt-2 text-4xl font-semibold leading-none text-[var(--foreground)]">{formatInTimeZone(punch.registeredAt, "America/Fortaleza", "HH:mm:ss")}</p></div><p className="text-right text-xs text-[var(--muted-foreground)]">{formatInTimeZone(punch.registeredAt, "America/Fortaleza", "dd/MM/yyyy")}</p></div><p className="mt-5 text-sm font-semibold text-[var(--foreground)]">{access.allowedUnit.name}</p><p className="surface-elevated numeric mt-4 rounded-xl p-3 font-mono text-sm text-[var(--foreground)]">Código: {punch.receiptCode}</p><p className="mt-3 text-xs text-[var(--muted-foreground)]">{punch.locationStatus === "INSIDE_RADIUS" ? "Localização confirmada" : "Registro disponível para revisão de localização"}</p></article>)}</div></div>;
}
